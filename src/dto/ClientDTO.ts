import { ClaimStatus, CompletionMode } from 'src/entity/Claim';
import { Client } from 'src/entity/Client';
import { Transit } from 'src/entity/Transit';

export class ClientDTO {
  constructor(client: Client) {
    
  }
}
